1. config when mounted into the docker contained does not work, since the link becomes broken
2. Add saved window templates (e.g. 3 windows, one on the left [ideally with claude opened already]), two on the left, where top is larger and is the code buffer and small on the bottom as a terminal window.
3. Move the ipython from home manager to system install
