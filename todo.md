1. Map :E to be the same as :e
2. config when mounted into the docker contained does not work, since the link becomes broken
3. When in terminal "<leader>s." opens stuff in the wrong window! Nothing opens in terminal window and the buffer gets open in some other random window 
4. Add Maximise toggle together with fullscreen toggle and some visual indication that the window is maximised.
